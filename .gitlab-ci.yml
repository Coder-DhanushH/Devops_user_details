stages:
  - build
  - test
  - deploy

build:
  stage: build
  image: maven:3.9.9-eclipse-temurin-17
  script:
    - mvn clean package -DskipTests
  artifacts:
    paths:
      - target/*.jar
    expire_in: 1 day
  only:
    - main

test:
  stage: test
  image: maven:3.9.9-eclipse-temurin-17
  script:
    - mvn test
  only:
    - main

deploy:
  stage: deploy
  image: docker:20.10
  services:
    - docker:dind
  before_script:
    - apk add --no-cache docker-compose
  script:
    - docker build -t userdetails:latest .
    - docker rm -f userdetails || true
    - docker run -d --name userdetails -p 8081:8080 userdetails:latest
  only:
    - main